const getFirstName = (fullName) => fullName.split(" ")[0];

const isEven = (num) => num % 2 == 0;

const getLength = (arr) => arr.length;

module.exports = {
  getFirstName,
  isEven,
  getLength,
};
